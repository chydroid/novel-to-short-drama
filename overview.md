# 小说转短剧工作台 · 交付说明

> **当前版本：v2.16**（大版本固定为 **v2** 不随迭代升级；一致性 2.0 + 配音为 v2 主基线，此后所有改进以 **v2.1 → v2.16** 小版本递进记录）。以下按从新到旧列出各次小版本迭代：

> **v2.16 更新（一键打包导出 + 剧集类型阈值预设 + 批量剩余进度持久化）**：
> 1. **一键打包导出（.zip）**——导出中心一键把 srt / md / csv / json / 转码命令打成一个包（本机有配音时可选连整轨 .wav 一起打）；自带极简 zip 实现（store 无压缩），不引外部库，保持单文件零依赖。
> 2. **剧集画面类型预设**——设置里可选「自动 / 现代都市 / 古装年代 / 动画二次元」，按题材调整主体占比与构图安全区的判定阈值：古装厚衣不再误报空镜、动画高饱和不再误报离谱；不选时与 v2.15 行为完全一致。
> 3. **批量剩余进度持久化**——暂停 / 停止后剩余镜号写入本机存档，收尾提示报准「还剩 N 个」，重开页面也有提示并在批量按钮上挂「（续跑 N）」；存档按生图 / 生视频 / 配音分类，互不覆盖。
> 4. **导出逻辑抽成纯函数**——buildSrtText / buildMdText / buildCsvText / buildTrackWav，单导与打包共用一份实现，避免两套结果。

> **v2.15 更新（导出中心 + 批量暂停续跑 + 主体占比改显著性近似）**：
> 1. **导出中心**——md/csv/json/srt/整轨配音/mp3 转码/合成包/fcpxml 集中到一个面板，按「出片 / 字幕音频 / 数据脚本 / 备份」四组排列，工具区只留一个入口按钮；按钮 id 与事件绑定不变，行为零变化。
> 2. **批量暂停 / 继续**——进度条加「暂停」按钮，暂停后不再取新任务、正在跑的几项跑完即停住，可随时继续；关页面也没关系，重新点批量即按「未生成 / 失败」续跑。
> 3. **主体占比改显著性近似**——不再是纯肤色判定（暗调夜戏、古装厚衣、强色偏会误报「看不到人物」），改取「肤色 ∪ 显著性（亮度偏离 + 边缘强度）」的较大者，风景空镜仍能识别。
> 4. **修掉一个 id 冲突**——导出中心最初撞上「AI 提取结果弹窗」的 exBox/exMask/exClose，已改名 exp*，并加了 id 全局唯一 + $() 引用存在性的防回归断言。

> **v2.14 更新（主体占比评分 + 批量进度条 + mp3 转码命令）**：
> 1. **候选图评分加「人物主体占比」**——用 YCbCr 肤色像素比例近似人在画面里占多大，挑掉「人物小得看不见」和「怼脸贴满画框」两头的废片；面板显示「主体 N」分。这是像素统计不是人脸检测，暗调 / 古装 / 空镜会偏低，仅作提示，设置里可单独关。
> 2. **批量任务进度条**——批量配音、批量生图/视频改为常驻进度面板（已完成/总数 + 失败数 + 进度条 + 停止按钮），不再刷屏 toast，收尾明确报告成功与失败数。
> 3. **mp3 转码命令一键复制**——浏览器端没有 mp3 编码器，不假装能导 mp3：给出可直接在导出目录跑的 FFmpeg 命令（单文件与批量两种），零额外依赖。
> 4. **综合分改为四维加权并规则化**——一致性 20% / 构图 20% / 主体 15% 固定，剩余归技术分；关掉的维度权重自动还给技术分。

> **v2.13 更新（构图安全区评分 + 单句失败重试 + 整轨优先通用容器）**：
> 1. **候选图评分加「构图安全区」**——自动检查底部字幕区是否被内容占满（烧字幕会糊）、主体是否被画框切断（半张脸贴边）、内容重心是否偏出画面，面板显示「构图 N」与对应中文标签。综合分改为三维加权 60/20/20，缺哪个维度就把权重还给技术分。
> 2. **逐句配音单句失败重试**——并发合成时某句遇到限流不再直接废掉整镜，按全局重试次数退避重试，仍失败才报错并指明第几句。
> 3. **整轨压缩导出优先通用容器**——优先输出 .m4a/AAC（剪映、PR、达芬奇都直接认），不支持再退回 opus / wav；不引入任何外部编码库。

> **v2.12 更新（候选图一致性评分 + 逐句配音并发 + 整轨压缩导出）**：
> 1. **候选图评分加「画风一致性」**——用色彩直方图比对候选图与本剧最近定稿镜头的平均色调，一致性分占综合分 25%，面板显示「一致性 N」与「调性一致 / 与全剧色调有偏差」。真·语义一致性需 CLIP 类模型、纯前端做不到，色调是最接近的可算代理，只作提示。设置里可单独关（关掉即退回 v2.11 的纯技术分）。
> 2. **逐句配音并发提速**——设置可选 1/2/3 路并发（默认 2），原来串行逐句要等好几轮；结果按句序下标回填后才拼接，顺序不会乱、缺句不串位。
> 3. **整轨配音可选压缩导出**——开启后导出 .webm/opus 而非 wav，体积约 1/10；默认关（压缩需实时录制，耗时≈成片总长），不支持时自动改存 wav。

> **v2.11 更新（候选图自动评分 + 配音压缩存储 + 字幕按真实句长对齐）**：
> 1. **候选图自动技术评分**——A/B 选优面板给每张候选算 0–100 分与点评（清晰度 / 曝光 / 对比度 / 饱和度），最高分标「★ 推荐」；只评客观技术项（偏糊、偏暗、过曝、发灰、色彩寡淡），审美留给人，跨域取不到像素则显示「—」不影响选图。
> 2. **逐句配音压成压缩格式存本机**——默认开启，用 MediaRecorder 把拼好的 PCM 编成 webm/opus 再落 localStorage，体积约为 wav 的 1/10（本机 localStorage 只有几 MB，不压缩存不了几镜）；不支持时自动退回 wav，带兜底超时。
> 3. **cue 级字幕与口型时间对齐**——跑过逐句配音的镜头，导出字幕按每句**实际音频时长**切分（句间静音归前一句，各句之和正好等于整段时长），字幕与嘴型严丝合缝；没配音或台词改过句数对不上时自动退回字数估算，不会算错时间轴。

> **v2.10 更新（批量 A/B 选优 + cue 级 TTS + 拆镜直出 cue）**：
> 1. **批量 / 流水线接入 A/B 选优**——设置新增开关，开启后批量生图与长篇流水线对每个镜头静默生成 N 版候选（不弹面板打断跑批），跑完提示逐镜点「选图(N)」挑；成本预估按候选数乘算，费用透明。
> 2. **cue 级 TTS 逐句配音（解决一镜多说话人）**——开启后每句 cue 按**自己说话人的角色音色**分别合成再拼接（OS 画外 / VO 内心带对应演法提示，句间 0.12 秒静音，整段降采样 24k 落 wav），不必再为不同说话人硬拆镜头。默认关闭（每句一次请求 + wav 体积大）。
> 3. **拆镜 / 剧本化直出 cue 格式台词**——拆镜提示词要求 `line` 直接输出 `【情绪】角色名：台词` / `(OS)` / `(VO)` / `旁白：`，一句一行、单句 ≤20 字自动断句；剧本化同步要求「角色名：台词」。结构化从源头成立，无需事后整理。

> **v2.9 更新（多版本 A/B 选优 + 台词逐句 cue 结构化）**：
> 1. **多版本一次生成 A/B 选优**——分镜卡新增「A/B 选优」：一次并发生成 2~3 版候选图（候选数在设置里调），弹窗并排对比、点选落图；落选候选与旧图自动进图像版本历史（⇄ 回退通用），关掉面板后镜卡保留「选图(N)」入口随时回来挑。
> 2. **台词逐句 cue 结构化**——台词字段新增逐句结构化（格式 `【情绪】角色名(OS)：台词`，支持 OS 画外 / VO 内心 / 旁白），「逐句编辑」弹窗逐句配说话人（角色名联想）、类型、情绪、断句；导出字幕按句拆条（时间按字数占比分配）、配音逐句注入情绪 + 剥掉说话人前缀 + 说话人留空自动取对白角色音色、质检新增「一镜多说话人」与「超长句断句」告警。老项目台词零迁移、完全向后兼容。27 项纯逻辑单测全通过。

> **v2.8 更新（对标 Jellyfish 全局风格防漂移 + 统一时长口径）**：
> 1. **项目级「全局统一画风令牌」**（对标 Jellyfish 全局风格防漂移）——设置里新增 `全局画风令牌`，填一段画风/年代/材质/光质描述即固定注入每个镜头的生图与生视频提示词，整剧从首镜到末镜保持同一视觉调性，不再各镜自由发挥而风格漂移。
> 2. **统一镜头有效时长口径**——`shotDur` 统一为「优先配音真实时长并 clamp 到 4–12 秒」，并让配音生成时把音频时长与 `时长秒`、render.sh 段长取同值，修复 SRT / 整轨配音 / FCPXML / 合成脚本四处时长各算各的、在有配音镜头时可能对不齐导致的音画/字幕漂移遗留缺陷。

> **v2.7 更新（细粒度质量与生产增强）**：对照火宝 v3.1 受控镜头语言、MoneyPrinterTurbo 字幕底与多版本选优，做四项细打磨：
> 1. **修复合成 seg 错位**——render.sh 与 FCPXML 的 `seg_N` 段号统一按「有图片或有视频」的镜连续编号，缺视频镜不再让两边错位剪错镜头。
> 2. **同镜图像版本历史 + 防顶掉**——重生成图片前先把旧图压入历史，可一键「切回上一版」；换图且有旧视频时警示「重新生视频」，不再静默覆盖出废片（对标 MoneyPrinterTurbo 多版本选优）。
> 3. **配音随备份**——导出 JSON 可选含每镜配音 base64，导入按镜号写回，换机/导入不再丢配音。
> 4. **镜头语言受控词表**——机位角度/运镜中英词典，生图与视频提示词自动映射规范英文 camera 语言；**字幕半透明黑底**（对标 MoneyPrinterTurbo）可在设置里开关。

> **v2.6 更新（第三次细粒度深度对标）**：再对照火宝短剧（角色提取 Agent）、魔因漫创（质检 Agent / 字幕样式）、ArcReel 等的**细粒度**能力清单，补齐六项（Playwright 回归 43 项断言全通过）：
> 1. **AI 从小说一键提取角色与场景（对标火宝角色提取 Agent）**——文本模型读全文，自动输出主要角色（性别年龄 / 发型 / 上下装 / 配饰 / 性格语气 / 英文一致性锚点 / TTS 音色建议）与主要场景（时间光线 / 关键陈设 / 英文锚点），预览勾选后批量写入设定表；已存在的卡自动跳过不重复。
> 2. **分镜质检 Agent（对标魔因漫创质检 Agent）**——一键体检整张分镜表，逐镜查出：台词字数与时长是否匹配（按可调的中文语速）、时长是否超出模型允许的 4–12 秒、缺角色/场景卡与设定图、生成失败、口型高风险、连续多镜同景别、全片总长过短；每条带「一键修复 / 建卡 / 重试 / 定位」按钮，修完自动重跑报告。
> 3. **提示词维度细化（光影 + 结构化外观）**——分镜新增「光影 / 时间 / 天气」字段，同时注入生图与生视频提示词；拆镜系统提示词要求模型输出 `lighting`；角色资产外观改为**结构化**（发型 / 上装 / 下装 / 配饰道具），无「常服」时自动拼装服装锁，保证定妆照与分镜图口径一致。
> 4. **提示词预览与改写（专业平台必备）**——每个分镜可「查看 / 改写」最终送给模型的生图与生视频提示词，改写后固定生效、可一键还原自动拼装（参考图仍自动附带）。
> 5. **字幕样式可配（对标魔因漫创）**——设置里可调烧录进成片的字幕**字体 / 字号 / 文字颜色 / 描边颜色粗细 / 底部边距 / 加粗**，同步应用到 render.sh 与 render.bat。
> 6. **导出剪辑时间线 .fcpxml（对标专业出片流程）**——导出可直接导入 Premiere / 达芬奇 / Final Cut 的时间线：按镜头顺序排好、时长对齐、台词写进备注，媒体引用与合成脚本落盘一致的 `seg_N.mp4`；还没生成视频的镜头不占位、可后补。
>
> **v2.5 更新（第二次对标 GitHub 高星项目改进）**：再对照 Toonflow（6.1k★）、魔因漫创 moyin-creator（4.5k★）的功能清单，补齐五项能力（Playwright 回归 22 项断言全通过）：
> 1. **并发任务队列（对标 moyin-creator multi-task parallel queue）**——批量生图 / 生视频 / 配音 / 口型由串行改为**并发池**，设置里可调并发路数（1–8）与失败自动重试次数（0–3，指数退避），失败的镜头会自动重试，另有「停止当前批量」。长篇流水线仍保持串行顺序（保证章节顺序与词表递进）。
> 2. **多 API Key 轮换（对标 moyin-creator 的 API key rotation）**——设置里可填多个 Agnes Key（每行一个），请求依次轮询；遇到 **429 限流 / 401、403 鉴权失败自动换下一个 Key 重试**，多账号分摊配额、抗限流。
> 3. **风格预设库（对标 moyin-creator 一键视觉风格切换）**——内置 8 套预设（写实电影感 / 国风古装 / 日系动画 / 3D 动画 / 水墨国漫 / 赛博朋克 / 黑白悬疑 / 青春校园），一键同时设定题材画风、全局提示词前缀、全局负向词。
> 4. **分镜编辑增强（对标导演模块可视化 Timeline）**——分镜表每行新增**上移 / 下移 / 复制 / 插入空镜**，操作后自动重排镜号；新增**撤销**按钮与 Ctrl+Z（快照栈最多 20 步，覆盖排序、复制、插入、删除）。
> 5. **成片合成增强（对标 S 级成片的转场与镜头语言）**——图片垫段带 **Ken Burns 缓慢推近/拉远运镜**（zoompan），镜头间 **段内淡入淡出转场**，输出**帧率与码率**可配。转场刻意采用「段内淡入淡出」而非跨段 xfade，**总时长不变，因此不会与整轨配音 / 字幕时间轴错位**；选「硬切」时视频段仍走 `-c copy` 快速拼接。
>
> **v2.4 更新（v2.3 代码完整性审计）**：对 v2.3 四大新功能逐一审读源码并回归验证，修复 1 处真缺陷 + 1 处精度提升：
> 1. **修复：章节剧本化结果不持久化**——`saveChapters` 原先只存章节元数据，刷新页面后所有「AI 剧本化」结果全部丢失，流水线会退回用原文拆镜并重复消耗模型调用量。现已把 `adapted` 一并写入本地存储，刷新后「已剧本化」标签、「用原文」还原按钮、剧本内容均完整保留。
> 2. **新增防串拆保护**——重新「识别章节」时，仅当章节文本未变化（长度一致）才保留旧剧本；文本改过的章节自动弃用旧剧本，防止拿旧剧本拆新文。
> 3. **提升：流水线成本预估精度**——启动前视频费用预估从固定「每镜 5 秒」改为按已拆镜头的真实时长累计（未拆镜时仍按 5 秒估）。
> 审计确认其余功能均为完整实现：用量计数覆盖全部 5 条生成路径（分镜图/设定图/视频/TTS 两种返回形态/口型）、BGM 混音（循环铺底/淡入淡出/削波保护）与合成脚本一致、合成包 render.sh/render.bat/合成清单 齐全、所有批量入口均有成本确认弹窗。回归验证 11 项断言全通过。
>
> **v2.3 更新（对标 GitHub 高星短剧项目改进）**：对照火宝短剧 huobao-drama（~14.8k★）、NarratoAI（~11k★）、ArcReel（~4.3k★）、Jellyfish 等高星项目的功能清单，补齐四大差距（Playwright 全流程验证 38 项断言全通过）：
> 1. **一键合成包导出（对标火宝 FFmpeg 整集拼接）**——「成片与导出」新增按钮，导出 `render.sh`（macOS/Linux）+ `render.bat`（Windows，含 chcp 65001）+ `合成清单.json`。脚本直接用 ffmpeg 读取各镜公网 URL：逐镜落盘（没视频的镜头自动用图垫段）→ concat 拼接 → 混整轨人声 + BGM（音量/淡入淡出）→ 烧 SRT 字幕，产出 **成片.mp4**。生成的 bash 已做语法校验。
> 2. **BGM 背景音乐轨（对标 NarratoAI）**——设置里填 BGM 音频 URL、音量（0-1）、淡入淡出秒数；导出「整轨配音 .wav」时自动循环铺底、按音量混合、首尾淡入淡出；合成脚本同样带上。
> 3. **用量与成本追踪（对标 ArcReel）**——生图张数 / 视频秒数 / 配音条数 / 口型次数自动计数，设置里可填四项单价，成片时间线实时显示用量与预估花费，可一键清零。
> 4. **章节 AI 剧本化（对标火宝 script_rewriter agent）**——章节卡新增「AI 剧本化」按钮：把小说正文改写成对白化、开头强钩子、紧凑节奏的短剧剧本（人物名经全局词表锁定），拆镜优先用剧本；流水线新增可选步骤「拆镜前先 AI 剧本化」；支持一键「用原文」还原。
> 5. **批量生成前成本确认（对标火宝批量确认弹窗）**——批量生图 / 生视频 / 配音 / 口型 / 长篇流水线启动前，弹窗列出将生成的数量与预估费用，确认后才开跑。
>
> **v2.2 更新（全面代码审计修复，11 处）**：
> 1. **一致性关键修复**——参考图按 URL 去重后再编号，消除「picture N 是谁」文字说明与实际图片错位的隐患
> 2. **音画同步补全**——URL 形态的配音也会解析真实时长并自动对齐镜头时长，字幕/整轨配音时间轴同步修正
> 3. **状态卡死修复**——刷新页面后「生成中」的图片/配音/口型/设定图自动重置（视频凭任务 ID 续轮询不受影响）
> 4. **体验与资源**——16:9 项目媒体框不再被裁成竖条；清空数据后示例不再复活；删除/清空/导入时自动释放本机缓存的音频；视频时长钳制 4–12 秒防接口拒单；设定图生成防双击重复计费；「清除 Key」补齐唇形服务 Key
>
> **v2.1 更新（长篇流水线 + 口型）**：
> 1. **长篇流水线**——整本小说粘贴 / 导入 .txt，自动按「第 X 章」切分（无标题按字数切段），勾选章节后一键跑完「拆镜 → 建/补设定卡 → 设定图 → 分镜图 → 配音 → 视频」。跨章用全局词表锁住角色/场景名，进度存本机、可断点续跑，支持暂停。
> 2. **口型同步**——新增口型风险判定（景别 × 是否说话人在画面）、AI 一键规避改写（把正面说话特写改成过肩/侧脸/反应镜头）、可选接入第三方唇形服务（设置里填 Base URL/Key，视频+音频自动提交合成）。

## 一、它解决什么

把你已有的几样东西串成一条流水线：**桌面 App 写好的小说 → 分镜脚本 → 角色/场景设定图 → 分镜图 → 分镜视频 → 配音 → 成片**。

- 小说文本：从桌面写作 App 复制粘贴，或导入 .txt
- 分镜拆解：页面调用你自己配置的文本模型（OpenAI 兼容接口）
- 分镜图：`agnes-image-2.5-flash`
- 分镜视频：`agnes-video-2.5-flash`（keyframe 首帧图驱动）
- 配音：小米 MiMo TTS（限时免费）
- 成片：时间线总览 + 字幕 / 整轨配音 / 视频清单，交给剪映 / PR

## 二、访问方式

- **在线链接（数据云端同步）**：https://www.workbuddy.cn/space/d/YuLFDfUvm1WS9iB4V40qM2
- 本地文件：`novel-to-drama-studio.html`（离线打开自动降级为浏览器本地存储）

| 表 | ID | 用途 |
|---|---|---|
| 小说转短剧·分镜表 | `AolzNRs7EEyxMBxDlX7LLU` | 分镜（含出场角色、场景、景别、情绪、说话角色、音频状态等） |
| 短剧·角色与场景设定表 | `BMl6SDoOP03s6IgjJHiOQJ` | 角色卡 / 场景卡（含多视图参考图、常服、性格、TTS 音色） |

## 三、一致性方案 2.0（角色不换脸、教室不换布局、性格不飘）

原方案每镜只挂「角色定妆照 + 场景空景图」，仍会漂移。2.0 做了五层加固：

### 1. 多视图设定图（关键改动）

| 卡类型 | 主图 | 副视图（新增） |
|---|---|---|
| 角色 | 全身正面定妆照（中灰背景，3:4） | **面部特写**，以定妆照为参考图生成，保证是同一张脸 |
| 场景 | 无人空景广角（主机位） | **反打机位**，以空景图为参考图生成，保证正反打布局一致 |

副视图**必须以主图为参考**生成，否则等于重新抽卡——这是最容易做错的一步。

### 2. 参考图优先级与上限可配

一次请求能吃的参考图有限，超出必须按重要性取舍。顺序：

```
角色定妆照  >  场景空景图  >  角色面部特写  >  已满意的基准帧  >  场景反打机位
```

设置里「单镜参考图上限」默认 **4**（模型最稳），角色多时可调到 6。

### 3. 基准帧机制（越往后越稳）

分镜图上点「☆ 标为满意」，这张图会成为该角色**后续所有镜头**的参考图之一——用的是真实成片里的样子，比定妆照更贴近输出分布。

### 4. 三重文字锁定

每个生图请求都强制拼入：

- **Outfit lock**：角色卡「常服」字段 → 防每镜换装
- **Shot size**：景别（远景/全景/中景/近景/特写/大特写）→ 防构图乱跳
- **Performance and emotion**：情绪/表演 → 同时喂给画面和配音
- **CRITICAL CONSISTENCY RULES**：脸型/发型/体型/服装/房间布局/门窗/光源一律不得改变，只允许改变机位、构图、动作、情绪
- **Avoid**：全局负向词 + 每镜自定义负向词

### 5. 一致性体检

设定页新增体检面板，逐项列出会导致漂移的隐患（缺定妆照、缺英文锚点、没写常服、没绑音色、角色名对不上卡……），每条都有一键处理按钮，并给出 0–100 的一致性得分。

### 正确顺序（别反）

```
① 剧本拆镜 → ② 角色与场景（生成设定图 + 副视图）→ ③ 生成分镜图/视频 → ④ 配音 → ⑤ 成片导出
```

## 四、配音与音画同步（小米 MiMo TTS）

### 流程

1. 角色卡绑定 **TTS 音色 + 风格标签 + 语速**（点「AI 补全设定描述」可一次性自动配齐服装、性格、音色）
2. 分镜填「说话角色」（留空 = 旁白，用旁白音色）
3. 点「生成配音」→ 调 MiMo TTS → 音频存本机并可试听
4. **镜头时长自动对齐到语音长度**（限制 4–12 秒），视频按该时长生成 → 音画同步
5. 语音比镜头长 / 镜头比语音长时，卡片上会给出黄色提示

### 音画怎么对上

- **字幕 .srt**：按每镜**真实音频时长**累加排时间轴（无配音的镜头用镜头时长）
- **整轨配音 .wav**：把各镜音频按顺序拼成一条轨（缺配音的镜头补等长静音），拖进剪映与各镜视频对齐 0 点即可
- 两者共用同一套时间轴，不会错位

### API 参数（已固化）

```
POST https://api.xiaomimimo.com/v1/chat/completions
{ "model":"mimo-v2.5-tts",
  "messages":[ {"role":"user","content":"音色/性格/情绪描述"},
               {"role":"assistant","content":"要合成的台词"} ],
  "audio": { "format":"mp3", "voice":"白桦" } }
```

- 待合成文本必须在 **assistant**；user 是语气指令，不出现在语音里；不支持 system
- 返回 `choices[0].message.audio.data`（base64）
- 音色：`mimo_default / 冰糖 / 茉莉 / 苏打 / 白桦 / Mia / Chloe / Milo / Dean`
- 认证头同时发 `Authorization: Bearer` 和 `api-key`，兼容不同版本
- voicedesign 模型不接受内置音色，页面会自动改为音色描述模式

## 五、四个模块

| 模块 | 做什么 |
|---|---|
| ① 剧本拆镜 | 粘贴小说 / 导入 txt → 设镜头数、时长、画风 → AI 拆镜输出画面描述、英文提示词、运镜、**景别、说话角色、情绪**、台词、时长；拆完自动建设定卡 |
| ② 角色与场景 | 角色卡（定妆照 + 面部特写 + 常服 + 性格 + TTS 音色）/ 场景卡（空景图 + 反打机位 + 关键陈设）；**一致性体检**；AI 一键补全全部设定 |
| ③ 生成图 / 视频 | 每镜带多视图参考图 + 文字锚点出图；以图为首帧出视频；支持满意锁定、失败重试、批量生成 |
| ④ 成片与导出 | 进度环、时间线、导出脚本 .md / 清单 .csv / 备份 .json / **字幕 .srt** / **整轨配音 .wav** |

## 六、使用前必做：填 Key

右上角「设置」里填四组配置，只存在本机浏览器：

1. **Agnes API Key** + Base URL
2. **文本模型**：Base URL / Key / 模型名（OpenAI 兼容）
3. **小米 TTS API Key** + Base URL（在 platform.xiaomimimo.com 申请，**与模型推理 Key 不通用**）
4. 三个「测试」按钮逐个验证

## 七、跨域（CORS）与代理

浏览器直连三家 API 都会被 CORS 拦截。启动零依赖代理：

```bash
node agnes-proxy.js --text https://api.deepseek.com --tts https://api.xiaomimimo.com
```

设置里改填：

| 配置项 | 值 |
|---|---|
| Agnes Base URL | `http://127.0.0.1:8787/v1` |
| 任务查询根地址 | `http://127.0.0.1:8787` |
| 文本模型 Base URL | `http://127.0.0.1:8787/text/v1` |
| 小米 TTS Base URL | `http://127.0.0.1:8787/tts/v1` |

代理只监听 127.0.0.1，不对外暴露。

## 八、API 关键参数（图片 / 视频）

**图片** `POST /v1/images/generations`
```json
{ "model":"agnes-image-2.5-flash", "prompt":"...", "size":"1K", "ratio":"9:16",
  "extra_body": { "response_format":"url", "image":["参考图URL", "..."] } }
```
`response_format` 必须放在 `extra_body` 里。返回 `data[0].url`。

**视频** `POST /v1/videos`
```json
{ "model":"agnes-video-2.5-flash", "prompt":"运镜+画面+情绪", "mode":"keyframe",
  "first_frame":"<分镜图URL>", "seconds":"5", "size":"720P", "aspect_ratio":"9:16", "n":1 }
```
创建后取 `video_id`，轮询 `GET /agnesapi?video_id=<ID>&model_name=agnes-video-2.5-flash`。Flash 版 `size` 只能 `720P`，`seconds` 为字符串 "4"–"12"。

## 九、数据安全与边界

- 分镜数据存资料库数据表，多设备同步；API Key、小说原文、音频本体只存本机 localStorage
- **配音音频只存在生成它的那台设备**（base64 太大不适合进数据表），换设备需重新生成或导出整轨
- 在线链接公开可访问，不要在页面里预填真实隐私内容
- 视频生成较慢，轮询最长 12 分钟判超时
- 每部剧目前一个项目一张表；多剧目可再加「剧目」字段

## 十、本次验证

真实 Chromium 跑了 34 项断言，全部通过：参考图优先级与上限、面部特写/反打机位的参考注入、景别/服装/情绪/负向词进 prompt、满意基准帧生效、TTS 请求结构（文本在 assistant、性格与情绪注入 user、音色取角色卡）、音频时长解析、镜头时长自动对齐、字幕时间轴连续累加、整轨总时长、全程无 JS 报错。
