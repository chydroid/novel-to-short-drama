# WorkBuddy 工作数据（会话记忆、本地脚本等）—— 严禁上传
.workbuddy/

# 临时验证 / 调试脚本（下划线前缀为本项目约定）
_*.js
_*.sh
_*.html
_*.json
# 临时验证的输出目录（E2E 产出的 xlsx/zip/json 样例）
_*/

# 密钥与本地配置（API Key 均在页面设置里填写，不入库）
.env
*.env
*.local

# 日志与系统文件
*.log
.DS_Store
Thumbs.db
desktop.ini

# 依赖目录
node_modules/
