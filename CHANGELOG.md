# 更新日志（Changelog）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格记录。

## [v2.9] - 2026-09-10

### Added
- **多版本一次生成 A/B 选优**：分镜卡新增「A/B 选优」按钮，一次并发生成 2~3 版候选图（设置里配候选数），弹出面板并排对比、点「选它」落为当前图；落选候选连同旧图自动进入 v2.7 的图像版本历史（⇄ 可回退），不静默丢弃任何一张已生成的图。关掉面板后镜卡会保留「选图(N)」入口，随时回来继续挑。
- **台词逐句 cue 结构化**：「台词旁白」仍是唯一落库字段（老项目零迁移），新增结构化序列化约定 `【情绪】角色名(OS)：台词`（OS=画外音 / VO=内心独白 / 旁白=旁白）。新增「逐句编辑」弹窗：逐句设置说话人（带角色卡名称联想）、类型（对白/旁白/OS/VO）、情绪与断句，保存回写台词字段。三处消费：
  - **导出字幕**：一键按逐句 cue 拆条（可在设置关闭），时间按各句预计语速占比在本镜时长内分配、尾句兜到镜末不留缝，对白带「角色名：」前缀；纯长文本自动按句号断句。
  - **配音（小米 TTS）**：「说话角色」留空时自动取第一个对白 cue 的角色音色；每句【情绪】逐句注入 TTS 风格指令；送 TTS 的文本剥掉「角色名：」前缀（音色已区分说话人，不念名字），多句用换行连接形成句间停顿。
  - **分镜质检**：新增「一镜多说话人」告警（单音色会读串，建议拆镜）与「超长句（>20 字）」断句提示。

### Changed
- 设置新增「生图候选数」（1/2/3，1=不启用 A/B）与「逐句字幕」开关（默认开）。

### 技术说明
- 候选图 `_imgCand`、版本历史 `_imgHist` 均为 `_` 前缀本地字段，不落资料库、不破坏 schema。
- `parseCues / fmtCues / cueEst / cuePrefix` 纯函数与 SRT 分配逻辑经 27 项断言单测全通过；`parseCues` 对无前缀老文本完全向后兼容（整段视为旁白，长文本自动断句）。

## [v2.8] - 2026-09-10

### Added
- 项目级「全局统一画风令牌」（`cfg.globalStyle`，设置抽屉新增输入框）：对标 Jellyfish 的全局风格防漂移。填一段画风/年代/材质/光质描述后，会固定注入每一个镜头的生图与生视频提示词，让整部剧从第一镜到最后一镜保持同一视觉调性。

### Fixed
- 统一镜头有效时长口径（`shotDur` 优先音频真实时长并 clamp 到 API 允许的 4–12 秒），修复了 render.sh 段长 / SRT / 整轨配音 / FCPXML 四处时长各自计算、在有配音的镜上可能对不齐导致音画/字幕漂移的遗留缺陷。
- 生成配音时把「音频时长」也 clamp 到 4–12 并回写，与 `时长秒`、render.sh 段长保持同一数值，杜绝超长语音在剪辑软件里拖长字幕的隐患。

## [v2.7] - 2026-09-09

细粒度质量与生产增强（对标火宝 v3.1 受控镜头语言、MoneyPrinterTurbo 字幕底与多版本选优）。

- 修复：合成包 render.sh 与 FCPXML 时间线的 seg 段号口径不一致 —— 有缺视频镜时两边 `seg_N.mp4` 会错位、剪辑导错镜头；现统一按「有图片或有视频」的镜连续编号
- 修复：重生成某镜图片会静默覆盖旧图、且不清下游 —— 引入同镜图像版本历史，旧图先入历史不丢失，可在镜卡一键「切回上一版」；已换图且有旧视频时给出「重新生视频」警示，不再静默出废片
- 修复：配音 base64 不随备份走，换机/导入后配音必丢 —— 导出 JSON 可选用「含配音」，导入按镜号自动写回
- 新增：镜头语言受控词表（机位角度 / 运镜 中→英双语映射），生图与生视频提示词自动输出规范英文 camera 语言（未命中词保留原文，不打断自由创作）
- 新增：字幕可选「半透明黑底」（对标 MoneyPrinterTurbo 字幕底，ASS BorderStyle=4 + BackColour），设置里开关、烧录时生效
- 技术：`toProps` 忽略 `_` 前缀本地字段（候选图历史等不落资料库、不破坏 schema）

## [v2.6] - 2026-09-09

第三次细粒度对标 GitHub 高星项目（火宝短剧角色提取 Agent、魔因漫创质检 Agent 与字幕样式、ArcReel）后的改进。

- 新增 AI 从小说一键提取角色与场景：读全文输出结构化设定卡（发型/上下装/配饰/性格/英文锚点/TTS音色），预览勾选后批量写入，已存在自动跳过
- 新增分镜质检 Agent：逐镜体检时长越界、台词念不完/拖沓、缺角色场景卡与设定图、生成失败、口型风险、连续同景别、全片过短，每条带一键修复
- 分镜新增光影/时间/天气字段并注入画面提示词；拆镜模型同步输出 lighting
- 角色外观改为结构化（发型/上装/下装/配饰道具），无常服时自动拼装服装锁，定妆照与分镜口径一致
- 新增提示词查看/改写：看最终送给模型的完整提示词，可改写覆盖、可一键还原
- 字幕样式可配：字体/字号/颜色/描边/边距/加粗，应用到合成脚本烧录
- 新增导出剪辑时间线 .fcpxml：可直接导入 Premiere/达芬奇/Final Cut，镜头顺序与时长已排好
- 回归验证 43 项断言全部通过

## [v2.5] - 2026-09-09

第二次对标 GitHub 高星项目（Toonflow、魔因漫创 moyin-creator）后的改进。

- 新增并发任务队列：批量生图/视频/配音/口型改为并发执行，可调并发路数与失败自动重试，支持停止当前批量
- 新增多 API Key 轮换：多个 Key 每行一个，轮询使用，遇 429/401/403 自动换 Key 重试
- 新增风格预设库：8 套视觉风格，一键设定画风、提示词前缀与负向词
- 分镜编辑增强：上移/下移/复制/插入空镜并自动重编号，支持撤销（按钮 + Ctrl+Z）
- 合成脚本增强：图片垫段 Ken Burns 运镜、段内淡入淡出转场、帧率与码率可配（转场不改变总时长，音画不错位）
- 回归验证 22 项断言全部通过，生成脚本通过 bash 语法校验

## [v2.4] - 2026-09-09

v2.3 代码完整性审计修复。

- 修复：章节「AI 剧本化」结果不持久化，刷新页面后全部丢失、流水线退回原文拆镜的问题
- 新增防串拆保护：重新识别章节时，文本变化的章节自动弃用旧剧本
- 提升：流水线启动前的视频成本预估改按真实镜头时长累计
- 回归验证 11 项断言全部通过

## [v2.3] - 2026-09-09

对标 GitHub 高星短剧项目（huobao-drama、NarratoAI、ArcReel 等）补齐功能差距。

- 新增一键合成包导出：render.sh / render.bat / 合成清单.json，ffmpeg 完成视频拼接、图片垫段、人声+BGM 混音、字幕烧录，产出成片.mp4
- 新增 BGM 背景音乐轨：URL / 音量 / 淡入淡出配置，WebAudio 混入整轨配音
- 新增用量与成本追踪：图 / 视频秒 / 配音 / 口型自动计数，可填单价，实时预估花费
- 新增章节 AI 剧本化：正文改写为对白化、强钩子剧本后再拆镜，支持还原
- 批量生成（图 / 视频 / 配音 / 口型 / 流水线）前新增成本确认弹窗

## [v2.2] - 2026-09-09

全面代码审计，修复 11 处问题。

- 修复参考图 URL 重复导致的「picture N」说明错位（按 URL 去重）
- 修复 URL 形态配音无真实时长的问题，音画同步与字幕时间轴随之修正
- 修复刷新后「生成中」状态卡死（自动重置，视频凭任务 ID 续轮询）
- 修复 16:9 媒体框被裁切、清空后示例复活、音频缓存泄漏、视频时长越界、设定图双击重复计费、唇形 Key 清理遗漏等

## [v2.1] - 2026-09-09

长篇流水线与口型同步。

- 新增长篇流水线：整本小说自动切章，一键跑完拆镜 → 设定卡 → 设定图 → 分镜图 → 配音 → 视频，跨章词表锁名，断点续跑
- 新增口型同步方案：风险判定 → AI 规避改写（过肩/侧脸/背身）→ 可选第三方唇形服务

## [v2.0] - 2026-09-08

v2 主版本基线（后续 v2.1–v2.6 均在此之上小版本递进）。

一致性方案 2.0 与配音。

- 一致性加固：多视图设定图（角色面部特写 / 场景反打机位，以主图为参考生成）、参考图优先级与上限可配、满意基准帧机制、服装/景别/情绪/负向词多重文字锁定、一致性体检面板
- 新增小米 MiMo TTS 配音：角色绑定音色，镜头时长自动对齐语音长度，字幕 .srt 与整轨配音 .wav 共用同一时间轴

## [v1] - 2026-09-08

首个版本。

- 剧本拆镜 → 设定图 → 分镜图 → 图生视频 → 导出的基础流水线
- 单文件 HTML + WorkBuddy 资料库云端同步，本地零依赖 CORS 代理
- 修复：iframe 沙箱内原生 confirm 失效导致的删除按钮无响应（改为自定义确认弹窗）
