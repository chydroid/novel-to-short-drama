# 更新日志（Changelog）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格记录。

## [v2.36] - 2026-09-12

### Fixed
- **长篇章流水线把失败章标成「完成」（重要）**——`runChapterLoop()` 里那一串步骤（拆镜、补设定卡、设定图、分镜图、配音、视频、口型）**内部全是「吞错型」**（自己 `catch` 掉、只写状态），几乎走不到末尾的 `.catch`，于是**无论这一章实际做成了什么，都会被打上 `c.st = '完成'`**。而 `runPipeline()` 的待办筛选条件正是 `c.st !== '完成'`——**失败章被永久跳过，那一章再也没人跑**，收尾还报「全部完成：本批 N 章已跑完」。现在改成**按事实对账**：新增 `chapterLeft(c, opt)` 数出「本章还剩多少该做没做的项」（只统计勾选的步骤），一章都没拆出分镜 → 标「失败：没拆出分镜」；还有剩余 → 标「部分完成：还剩 N 项」（不等于「完成」，续跑会重跑它）；收尾也据此如实汇报。
- **重跑「部分完成」的章会让分镜翻倍**——`splitChapter()` 是无条件「续号追加」（`maxNo + i + 1`），原来失败章永不重跑所以没暴露；上面一改就会暴露。现在拆镜前先看该章有没有分镜，有就跳过拆镜（返回 `null`），提示也如实写「已有分镜，跳过拆镜」。顺带修掉既有隐患：手动重跑已完成的章同样不会重复拆镜。
- **首页「续跑流水线」不吃段内失败**——`runResumeStep()` 只看 `batchRun()` 返回的「剩余数」，而剩余数只反映**有没有被叫停**，全失败也是 0。于是「补图全失败 → 出视频没图可跑 → 配音照跑」整条串下来，用户仍以为一切正常。现在 `batchRun()` 把本段失败数记进 `state.lastBatchBad`，续跑流水线逐段累加到 `state.pipeBad`，收尾明确汇报「流水线跑完，但有 N 个任务失败」。

### Added
- **章节列表顶部给出对账汇总**：`共 N 章 · 已完成 X · 失败 Y · 部分完成 Z（点「开始流水线」只会重跑这些有问题的章）`，并给失败章加红标、部分完成章加橙标——原来这两种状态都是默认灰，混在长列表里根本看不出来。

## [v2.35] - 2026-09-12

### Fixed
- **「失败自动重试」一直是摆设（重要）**——`genImage()` / `genVideo()` / `genAudio()` 这些 worker 内部自己 `catch` 掉错误、只把状态字段写成「失败」然后**正常 resolve**，Promise 层面永远是成功的。而 `runPool()` 只在 worker **抛错**时才重试，于是配置里设的「失败自动重试 N 次」**一次都没跑过**（确认框上却写着这句话）。现在 `runPool` 支持 `opts.isFail(item)`：跑完一项按**最新内存状态**再判一次，判定失败就照旧退避重试，重试到头仍失败则记进 `state.poolFail`。
- **批量的失败数恒为 0（重要）**——`batchRun()` 收尾用 `list.filter(s => s[stKey] === '失败')` 数失败，而 `list` 里是**跑批那一刻的对象引用**，`patchShot()` 却是「整表 `map` 出新对象」——旧引用上的状态永远是旧值。结果：哪怕全部失败，进度条上的失败计数也是 0，收尾一律报「批量任务已全部完成」/「配音全部完成，共 N 条」。现在按 `_id` 取当前镜再数（`shotById`），进度条逐项同理。
- **「重试失败项」一律报「重试完成」**——同上原因，且这个入口连进度条都没有（不能暂停 / 停止）。现在开批量条、按当前状态如实汇报「仍有 N 个失败」，并让重试真正生效。
- **设定图批量生成一律报「全部完成」（重要）**——`genAsset()` 同样吞错，几张失败都照样提示「设定图全部完成，现在可以去生成分镜图了」，用户真去了，出来的分镜图**全都没有参考图**，一致性 2.0 直接失效。现在按当前状态数失败并明确提示「带着失败的设定图去生分镜图会丢参考图」；流水线里的静默版也补一句提示（不打断流水线）。

### Added
- `runPool()` 统一失败记账：`state.poolFail`（每轮开头清零），`onOne(done, total, item, err)` 多带一个真实错误对象，调用方不必再猜。

## [v2.34] - 2026-09-12

### Added
- **撤销 / 重做按钮有可用态了**：原来两个按钮永远可点（点了才弹一句「没有可撤销的操作」），现在按栈里剩几步置灰，并把步数写进 title（「还可撤销 3 步」/「可重做 1 步」）。跨刷新恢复出来的栈尤其需要这个提示——否则用户根本看不出还有后悔药。
- **叫停后如实汇报**：v2.32 加了「停止」，但各处的收尾只判 `fail`，中途停掉会被当成「完成」（例如停止清空后照样走「清空成功」分支）。现在 `rep.stopped` 也参与判断，清空分镜 / 清空设定卡 / 导入 / 拆镜都会明确说「已停止：进了 x 条，还剩 y 条没动」。

### Fixed
- 叫停清空分镜时**不再顺手清本机音频**——原来只判 `!r.fail`，被叫停（还有镜头没删）也会把音频清掉，剩下的镜头就没配音了。现在改为 `!r.fail && !r.stopped` 才清。

## [v2.33] - 2026-09-12

### Fixed
- **「移动 / 复制 / 插入分镜」重排镜号失败会静默报成功（重要）**——这三处是 v2.30 那轮排查的漏网之鱼：
  - `moveShot()`：`arr.reduce(chain...)` 一条失败整链断（后面的镜号全不重排），`catch` 里只 `console.error`，**收尾照样 toast「已移动，镜号已重排」**——镜号实际已乱，用户要过很久才发现顺序不对。改 `runSerial` 串行 + 按成功/失败汇报；有失败才重新拉一次云端（成功的常见路径不多花请求）。
  - `dupShot()` / `insertShotAfter()`：`Promise.all(...).catch(function(){})`——① 几十条 `updateRecord` **并发**轰接口（容易撞限流、顺序也没保证）；② catch 是**空的**，重排失败**完全静默**，照样提示「已复制该镜」/「已插入空镜」，结果镜号直接重复。改串行 + 失败可见，且**重排有一条失败就取消这次复制/插入**：镜号乱了比少一镜更难收拾。

### Changed
- 这三处统一走 `runSerial(jobs, {label:'重排镜号'})`，与其余批量写口径一致。

## [v2.32] - 2026-09-12

### Added
- **重做（redo）**：撤销不再是单程票。撤销时把「现状」压进重做栈，重做再把它放回去；重做本身也占一格撤销，可来回切换。入口：撤销按钮旁边的「重做」按钮 + **Ctrl+Shift+Z / Ctrl+Y**。重做栈与撤销栈用同一套存法（同样落本机、同样限 3 条 60 万字符、同样恢复前校验 id）。**一有新改动就清空重做栈**——否则重做会跳到一个已经不存在的分支上。
- **批量任务可以叫停**：`runSerial(jobs, {tag})` 给这批任务起名字，同步条右侧冒出「停止」，点了之后**剩下的任务全部跳过**（正在跑的那条不半途掐断，收尾照常重新拉云端对齐界面）。已接上：清空分镜 / 清空设定卡 / 导入 / 拆镜 / 批量新增 / 撤销重做。汇报里新增 `stopped` 字段。
- `saveExtracted`（抽取结果批量写入）、`clearAllAssets`、`addShotsBulk` 也补上进度文案。

### Fixed
- **「补设定卡」在流水线里跑在 AI 补全描述之后（重要）**：`syncAssetsFromShots()` 云端是异步写，却同步返回「计划张数」，流水线紧接着 `loadAssets()` → `fillNewAssets()`（AI 补全设定描述），**经常发生新设定卡还没建好就开始补描述** → 新建的角色卡 / 场景卡空着描述直接进生图，一致性全崩。现在它**返回 `Promise<实际补齐张数>`**，三处调用（拆镜收尾、长篇流水线、提取按钮）都改成 `.then()` 等真正写完再往下走；「已提取 N 张」也改成按实际建成的张数报。
- 提取按钮的两个入口合并为 `pickAssetsFromShots()`，不再各自复制一份「同步取返回值」的写法。

### Changed
- 撤销 / 重做的「拿快照对齐云端」逻辑抽成 `restoreSnap(snap, label)` 共用（原来只有撤销一份，重做会复制一遍 diff）；撤销栈的落盘/恢复也抽成 `saveSnaps/loadSnaps`，撤销与重做共用。

## [v2.31] - 2026-09-11

### Added
- **撤销快照持久化**：撤销栈原来只活在内存里，**刷新页面 / 误关标签页就全没了**——偏偏「清空 / 覆盖导入 / 批量改」最需要后悔药。现在快照会落一份到本机（`saveUndo()` / `loadUndo()`），启动时在拉完镜头后恢复。整表快照不能无节制写：只落最近 `UNDO_SAVE=3` 条、总长过 `UNDO_MAX` 从旧往新丢、撞上配额再逐条丢到塞得下为止（本机音频 base64 才是大头，绝不能让撤销数据把正经数据挤出存储）。恢复前会校验：快照与现状的镜头 id 必须「一边包含另一边」（纯删或纯增），对不上说明数据换了一批（换设备 / 别人改过），直接作废——**不能拿历史覆盖现状**。
- **撤销覆盖面扩大**：原来只有「移动 / 复制 / 插入 / 删单镜」会存快照。新增 4 个一键改几十镜的入口：清空分镜、覆盖导入（`runImport`）、批量选最高分（`autoPickRun`）、体检一键修复（`applyHealthFix`，有修复项才存）。覆盖导入内部调 `clearAllShots(true)` 跳过重复快照，撤销不用按两下。
- **`runSerial(jobs, opts)` 支持进度文案**：传 `{label:'清空分镜'}` 且条数 ≥ 8 时，右上角同步条实时显示「清空分镜 3/20」——几十条的活儿原来只有一个转圈，看着像卡死。`setSync(mode, msg)` 新增临时文案参数，`renderSync()` 优先显示它。已用于清空 / 撤销 / 导入 / 拆镜 / 补设定卡。

### Fixed
- **补设定卡 / 载入示例的批量写入同样是「一条失败整链断 + 谎报」**：`syncAssetsFromShots()` 云端段用 `todo.reduce(chain...)` 且无 `catch`——第一条 `addRecord` 失败后面全不建、静默 unhandled rejection，外面还按 `todo.length` 报「已从分镜提取 N 张设定卡」。`seedDemo()` 两段 reduce 同理，示例只载入一半（有角色卡没分镜或反过来）仍报「已载入示例」。两者都改走 `runSerial`，跑完按**实际成功条数**汇总，失败才补提示（成功沿用调用方原有提示，不重复打扰）。
- **删除设定卡失败既不打日志也不说原因**：`delAsset()` 的 `catch` 只有一句 `toast('删除失败')`，且没有 `loadAssets()`，界面看着像删了其实没删。现补 `console.error` + `setSync('off')` + 具体原因；`addAsset()` 的「新增失败」同样补上原因。
- **设定图批量生成可能静默挂住**：`batchAssets()` 末端没有 `catch`，`loadAssets()` 之类抛错会让整条链 reject，用户一直等到天荒地老；现补终端提示，并顺带给主图 / 副视图两轮加进度。`batchAssetsQuiet()`（流水线用）更危险——一段 reject 会把整条流水线永远挂在「进行中」，现统一吞掉并记日志。
- `resetStuck()` 的设定卡侧由 `reduce` 链改 `runSerial`：原来一条失败整链断，后面的卡一直卡在「生成中」。

## [v2.30] - 2026-09-11

### Fixed
- **破坏性批量操作「部分失败」会静默或被整链中断（重要）**：与 v2.29 修的是同一类问题，只是发生在「增 / 删 / 撤销 / 导入」而不是「改」。这些地方原来都是 `ids.reduce(chain...)` 串行链——**一条失败整条链断掉**，后面的全不执行；有的甚至没有 `catch`（unhandled rejection、UI 不刷新、无任何提示），有的只 `console.error` 把错误吞掉（如撤销：只成功一半却照样提示「已撤销」，用户完全不知情）。现抽出通用串行执行器 **`runSerial(jobs)`**：每条**独立 catch**、失败不中断，跑完返回 `{ok, fail, errs}`，由调用方统一收尾（重新拉云端让 UI 与真实状态对齐 + 明确报「成功 x 条、失败 y 条」）。已应用于：`clearAllShots` / `clearAllAssets`（清空）、`undoShots`（撤销）、导入备份（设定卡 + 分镜两段）、拆镜、抽取结果批量写入、批量新增分镜。
- **`delShot()` 云端删除失败也会清掉本机配音**：原来一上来就 `purgeAud([id])`，若云端删除失败，音频已经被清且不可恢复。现改为**删除成功才清**；清空分镜同理——有失败就保留音频（没删掉的镜头还要用）。
- 批量新增 / 拆镜 / 抽取写入不再**谎报条数**：原来固定返回 `arr.length`，失败时也照样报「已写入 N 个」。现按**实际成功条数**汇报。

### Added
- `runSerial(jobs)`：串行执行一组云端写操作，每条独立 catch、失败不中断、返回成功/失败统计。与 `bulkPatch()` 是同一类问题的两面——`bulkPatch` 管「改」，`runSerial` 管「增 / 删 / 撤销」。

## [v2.29] - 2026-09-11

### Fixed
- **批量写云端时一条失败会连累后面全部、且改动静默丢失（重要）**：`bulkPatch()` 的云端串行链原本是「一条 reject → 整条链断掉」，于是**失败项之后的条目一个都不再推送**，而内存已被改、本地又只在无云时落盘——刷新页面（重新拉云端）改动全没，用户还毫不知情。现每条独立 `.catch`：失败项收进 `state.bulkFail`，其余继续推送；并且**推送前先落一次本地**做兜底，收尾按成功/失败数汇总，有失败才提示（`toast` + 首页红色「未同步」卡），全部成功保持静默不打扰。
- 首页新增 **「重试写入」** 入口（`retryBulkFail()`）：把失败清单重新投一次，仍失败则清单回填、卡片还在，可反复重试；也可「忽略」（改动仍在本机）。

### Performance
- **`bulkPatch()` 推广到三处「N 次循环写库」**——原先这三处都是先算完全量更新、再逐条 `patchShot()`：
  - `scanLipRiskCore()`：扫全片口型风险等级；
  - `avoidLipCore()`：AI 规避改写后批量写回（画面变了要重生成的那批）；
  - `resetStuck()`：把卡在「生成中」的镜头 / 设定图重置。
  改为收集 `{id, obj}` 后一次 `bulkPatch()`，代价从 N ×（全量序列化 + 6 面板重排 + N 个云端请求）降为 **1 次落盘、1 次刷新**；改写命中 0 条时不再空刷一遍、也不再报「已改写 0 个」。

### Added
- **体检明细记住分组折叠状态**：各问题类型的展开/收起记进 `cfg.healthOpen`，下次打开体检明细保持上次的视角（没记住的类型回落「硬性组默认展开」）。`<details>` 的 `toggle` 事件不冒泡，故在 document 上用**捕获阶段**监听，只认带 `data-hg` 的分组，不影响别处。

## [v2.28] - 2026-09-11

### Performance
- **批量落库 `bulkPatch()`：一键修复 / 批量选优不再「逐条重排整页」**——原来逐条 `patchShot()` 在批量场景的代价是 N ×（全量 localStorage 序列化 + `refreshAll()` 重排 6 个面板 + 云端 N 个并发请求），一键修复 200 条就是上千次重排、200 个并发请求。现新增 `bulkPatch(updates)`：先按镜**合并同一镜的多条更新**（某镜既缺时长又缺章节 → 合成一次写），再**一次改内存、一次落盘、一次刷新**；云端改为**按顺序串行**推送（保持写入顺序、不并发轰接口）；目标镜已被删除的条目不落盘也不推云。「一键自动修复」与「批量选最高分」的收尾都改走它。

### Added
- **体检明细按问题类型分组折叠**——明细从「按镜号平铺」改为按问题类型分组（`healthGroupRows()` 纯函数），硬性组在前、同档按条数降序、组内保持镜号顺序；每组一个 `<details>` 可折叠（原生折叠、零 JS 状态，硬性组默认展开），组头显示条数并给能本地推定的三类（没时长 / 没章节 / 有台词没角色）打「可自动修复」标（`FIX_KIND_OF`），方便一类一类集中处理；每行「去改」与顶部「一键自动修复(N)」「只补时长」保持不变。

### Changed
- 抽出 `candCommitObj(s, i)`：「选第 i 版候选」的提交口径（写 URL / 置已完成 / 清候选 / 旧图入历史 / 标记画面已变）现由**面板 `pickCand`、`commitBestCand`、批量路径三处共用**，去掉重复实现；改口径只需改一处。
- `fixHealthAuto()` 落库后不再自行 `renderToday()`——刷新交给 `bulkPatch()`，避免同一次修复渲染两遍。
- 同类隐患排查（v2.27 那个 Promise 误用的变体）：全文件 **22 处** `askConfirm` / `askInput` 调用**全部**走 `.then`，无新的「Promise 当同步值用」；回归守卫维持。

## [v2.27] - 2026-09-11

### Fixed
- **确认弹窗点「取消」也会照样改数据（重要）**：`askConfirm` 返回的是 **Promise**，但 v2.25 的「批量选最高分」与 v2.26 的「一键自动修复 / 只补时长」把它当布尔做否定判断（`if(!askConfirm(...)) return`）——Promise 恒为真、取反恒为假，于是**用户在确认框点「取消」，操作仍然执行并写入数据**。现改为 `askConfirm(...).then(function(ok){ if(ok){ ... } })`；并把执行体抽成 `autoPickRun(list)` / `applyHealthFix(plan)`，落库那一半不再和确认弹窗耦合，便于单测。测试新增回归守卫：全文件不得再出现该误用写法，且「取消」时两个入口都不得落库（`_askOkVal=false` 断言）。

### Added
- **体检明细可点跳 + 就地修复**：体检明细从「只读清单」升级为可操作——每条问题右侧加「去改」按钮，点一下**关掉弹窗、切到生成页、展开并滚动到那一镜**（复用新抽出的 `locateShot()`，与原「去修第一个」「剩余镜定位」同一套定位逻辑）；明细顶部给「一键自动修复(N)」「只补时长」两个快捷动作，修完**或取消都自动重列清单**（`fixHealthAuto()` 新增可选 `after` 回调），方便一条条核对还剩什么。
- **章节推断改为双向（承前 → 承后）**：`healthFixables()` 补章节时，优先「承前」取该镜之前最近的非空章节；若它前面一条章节都没有（比如前导镜头群），则「承后」承接其后最近的非空章节（预扫一张后缀表）。整片都没章节才留给人。

### Changed
- `fixHealthAuto(kinds, after)` 增加第二个可选参数；`hjump` / `leftjump` 改用 `locateShot()`，去掉三处重复的「setView + scrollIntoView」代码。

## [v2.26] - 2026-09-11

### Added
- **关键字段体检「一键自动修复」**：把 v2.25 的「补齐时长」升级为覆盖**全部能靠本地规则推定**的缺料项，一次确认批量修完并分类汇报（`healthFixables()` 出修复计划 → `fixHealthAuto()` 落库）。三类规则：① 没时长 + 有台词 → 按台词字数估（沿用 `estShotDurFromText`，4–12 秒）；② 没章节 → **承前**取该镜之前最近的非空章节（按全片顺序，与页面筛选无关，不猜未来）；③ 有台词没角色且该镜「出场角色」恰为 1 人 → 只能是他，直接填入。**缺画面描述 / 无生图提示词无法本地推定，一律不动、明确告知需人工补**；体检卡按钮显示可修总数（`一键自动修复(N)`），`healthFixSummary()` 给出「时长 x、章节 y、角色 z」分项摘要。原 v2.25 的「只补时长」入口保留为细粒度子集（`fixDurationsAuto()` = `fixHealthAuto(['dur'])`）。
- **生成页「候选待选」快筛**：筛选下拉新增「候选待选」（`viewShots` 增 `cand` 模式，命中 `_imgCand` 非空的镜），配合首页候选卡的「去挑（只看待选）」按钮——一键把生成页筛成只剩有候选待挑的几个镜，想手挑的人不必在整张分镜表里翻；该模式同样可用于「存为视图」与一键打包的状态二次筛选。

### Changed
- 首页体检卡按钮由「补齐时长(N)」改为「一键自动修复(N)」（`data-act` 由 `hfixdur` 升为 `hfixall`）；`hfixdur` 委派分支保留，供细粒度调用。

## [v2.25] - 2026-09-11

### Added
- **候选图「批量选最高分」**：生图开了 A/B 选优（一次多版候选）后，首页在「有候选待挑」时给出「批量选最高分」入口——对每张有候选的镜，复用与「A/B 选优」面板**完全一致**的评分口径（清晰度 / 曝光 / 一致性 / 构图 / 主体）自动提交最高分那版，省掉「跑完批量 → 逐镜打开面板点推荐」那一趟；取不到像素（跨域）的镜头自动跳过、交回你手动选，进度走批量进度条。把「选第 i 版」的提交动作抽成 `commitBestCand()`，面板与批量共用同一份，不会两套逻辑各写各的。
- **关键字段体检「一键补齐时长」**：体检卡在「有『没时长』且『有台词』的镜」时给出「补齐时长(N)」按钮——按台词字数（语速，默认 4.5 字/秒，与字幕 cue 同一套口径）自动估算并填入时长（clamp 4–12 秒，对齐 `shotDur`），能估的批量填、估不了的（无台词的空镜）不动，避免误改。与「去修第一个」互补。

### Changed
- 无。

## [v2.24] - 2026-09-11

### Added
- **生成页筛选视图可重命名 + 拖拽排序**：视图 chip 支持「✎ 重命名」（只改显示名，不影响存的筛选条件），并可拖动调整先后顺序（顺序即 `cfg.filterViews` 数组顺序，直接落盘）；空态提示补「拖拽可排序、✎ 可重命名」。删除仍是 v2.23 的 ✕。
- **一键打包「汇总表 .xlsx」多工作表合并导出**：导出中心新增「汇总表」勾选（默认不勾），勾上后把当前范围的 视频清单 +（勾 `附带体检` 时）关键字段体检 +（勾 `附带剩余清单` 时）各类型剩余清单 合并成**一张多工作表 Excel**（工作表名：视频清单 / 关键字段体检 / 剩余×清单），复用 `buildXlsxSheets()` 与已验证的极简 `xlsxBuild()`（零依赖）写进打包 zip（`短剧数据汇总.xlsx`）。已用 Python `openpyxl` 真机验证多工作表工作簿可正常打开、表头加粗、首行冻结、列宽自适应。
- **首页流水线「自定义步骤」**：续跑卡改为 4 个步骤开关（生图 / 生视频 / 配音 / 对口型，缺省图/视频/配音勾、对口型不勾，写 `cfg.pipelinePick`），可只勾想跑的段；「运行选中流水线」按勾选顺序串跑（图→视频→配音→对口型天然串行，某类无剩余即跳过该类），保留原「全串跑」入口（`runPipelineCustom()` / `togglePipelineStep()`）。
- **一键打包「按镜头状态二次筛选」**：导出中心新增状态下拉（全部 / 未生图 / 未生视频 / 失败 / 已完成），在「打包范围」基础上再砍一刀只导某一状态的镜头（`cfg.bundleStatus`，默认 all）。
- **预设与套餐「云端同步一键包」**：导出中心新增「⑤ 预设与套餐」段——把 镜号预设 + 类型套餐 + 筛选视图 合并成一个 JSON 包导出（`exportPresetsPkg()`），存进资料库 / 网盘即相当于云端同步；导入时同名覆盖、新名追加、空条目跳过并计数（`importPresetsPkgText()`），一次迁整套配置不必分别导三种。

### Changed
- 首页续跑卡从「四类分别列出 + 串跑」重写为「步骤开关 + 运行选中 / 全串跑」两种入口，自定义性更强。

## [v2.23] - 2026-09-11

### Added
- **生成页多套「筛选视图」**：把当前筛选条件（全部 / 未生图 / 按镜号选区等）存成命名视图，生成页顶部出现视图 chips，点一下即切回那套筛选并恢复当时的滚动位置；常用几套视角不必每次重选，多人协作也能共享同一套「只看这片」的视角（`cfg.filterViews`）。
- **xlsx 表头加粗 + 冻结首行 + 列宽自适应**：导出体检报告 / 剩余清单的 .xlsx 时，标题与表头行自动加粗、首行冻结（下滑不丢表头）、列宽按内容自适应；已用 Python `openpyxl` 实测确认加粗与冻结生效、工作簿可正常打开。
- **续跑流水线扩到「图→视频→配音」三段，并可顺带对口型**：首页续跑区在三类都有剩余时给出「串跑」入口，按 图→视频→配音 顺序接力（同一批镜一次跑完），跑完可选地给高口型风险镜对口型；每段开跑前各自校验所需 Key，中途暂停就停在当前段。
- **一键打包「只打这几样文件」勾选**：导出中心列出 字幕 / 脚本 / 清单 / 数据 / 转码命令 五个勾选（默认全勾），可只打需要的几样；配合范围 + 附带体检 / 剩余清单，协作发包更精准（`cfg.bundlePick`）。
- **镜号预设支持导入 / 导出 JSON**：与类型套餐一样可导出成文件、再导入合并（同名覆盖、新名追加、空条目跳过并计数），换设备或多人共用一套镜号区间不必重填。

### Changed
- 续跑卡覆盖「生图 / 生视频 / 配音 / 对口型」四类，按类型分别列出与续跑；批量对口型改走统一的 `batchRun()`，获得进度条、暂停与剩余存档。

## [v2.22] - 2026-09-11

### Fixed
- **修复自带 zip 实现的多文件偏移错误（v2.16 引入，影响所有「一键打包导出」）**：`zipStore()` 预分配空间时把「本地头 + 数据」与「中央目录项」两段一起累加给下一项当起点，导致第 2 个文件起的 `header_offset` 全部偏大（第一个文件正常，后面的在解压软件里报 `bad magic number`，只有第一个文件能解出来）。现在两段分开累加：先按本地段得到每项偏移，再单独累加中央目录与 EOCD 总长。回归新增 9 项断言（逐个本地头 magic、中央目录记录的偏移必须等于实际位置、EOCD 条目数），并用 Python `zipfile` + `openpyxl` 实测生成的工作簿可正常打开。

### Added
- **镜号表达式支持 `@预设名` 引用**：已存的镜号预设可以直接被别的表达式引用，如 `@夜戏`、`1-20,@片头,!@片尾`。展开最多 2 层（自引用 / 互相引用不会转圈），找不到同名预设就忽略该段（与写错段一致，不报错）。配合 `!` 前缀可整段排除。
- **镜号预设可排序 + 可直接筛选生成页**：预设按钮新增「↑ 上移」（列表顺序即常用度，自己排）与「筛」（切到「③ 生成图 / 视频」页，只显示该选区命中的镜头并滚动到第一个）。筛选下拉新增「按镜号选区」项，表达式存在 `cfg.filterExpr`（与导出范围同一套写法，所以 `@预设`、`#章节` 都能用）。
- **手写极简 xlsx 导出**：新增 `xlsxBuild()` / `downloadXlsx()`——xlsx 本质是 zip + 几段固定 XML（`[Content_Types].xml` / `_rels/.rels` / `workbook.xml` / `workbook.xml.rels` / `worksheets/sheetN.xml`），复用已有的 `zipStore` 即可，**不引任何表格库**（守住单文件零依赖）。单元格用 `inlineStr`（省掉 sharedStrings 部件），数字单独走 `<v>`（Excel 认得出是数值、可直接排序）；工作表名自动剔除 Excel 非法字符并截断 31 字符；XML 非法控制字符先剔掉（否则 Excel 报文件损坏）。导出中心新增「导出体检报告 .xlsx」「导出剩余清单 .xlsx」（后者一类一张工作表）。
- **一键打包可选附带体检报告 / 剩余清单**：导出中心新增两个勾选（`cfg.bundleHealth` / `cfg.bundleLeft`，默认不勾，包保持精简），勾上后把 `关键字段体检.csv` 与各类型的 `剩余×××清单.csv` 一起打进 zip——发给协作者时一份包看全，不必再单独导两次。
- **类型套餐支持导出 / 导入 JSON**：`exportGenrePresets()` 把自定义套餐导成 JSON 文件；`importGenrePresets()` 走隐藏文件选择框读回，`mergeGenrePresets()` 校验并合并（同名覆盖、新名追加、无效条目跳过并计数）。换设备或多人共用一套调好的配方时不必重新手配；内置三套是代码内置的，不受影响也不会被覆盖。
- **整轨配音体积建议**：导出预览新增一行——整轨 wav 多大、转压缩版（.m4a / mp3）约多大、能省多少百分比；已开「整轨压缩」时如实说明，整轨超过 20 MB 且未开压缩时给出明确提示（`trackAdviceTxt()`）。
- **续跑流水线「图→视频 串跑」**：生图与生视频同时有剩余时，首页续跑区出现「图→视频 串跑」按钮，按「先补图 → 自动接生视频」一次跑完，省掉「等图跑完 → 回来再点一次」那一趟。中途暂停则停在那一段，不会硬着头皮往下跑。新增 `leftShotList()`（把剩余存档映射回镜头列表，续跑与流水线共用）、`resumePipeline()` / `runResumeStep()`。

### Changed
- `parseShotRange(str, depth)` 增加可选的展开层数参数（默认 0），用于 `@预设` 引用的递归保护。
- 测试依赖图 `FN_DEPS` 新增 13 个函数；新增 84 项断言（441 → **525 项全过**）。

## [v2.21] - 2026-09-11

### Added
- **剩余镜号一键存为「选区」**：新增纯函数 `nosExpr()` 把一串镜号压成区间表达式（`[1,2,3,5,8,9]` → `1-3,5,8-9`，自动排序去重、忽略非数字）。续跑卡新增「存为选区」按钮，把当前该类型的剩余镜号直接存成一个镜号预设——暂停后不必手抄镜号，以后想在导出里「只导这批镜」直接套用。
- **镜号预设显示命中镜数**：预设按钮上以括号标注该表达式在当前分镜里命中多少镜，套用时 toast 也报出命中数。**套用前就知道选区对不对**，不用打完包才发现导了空包或漏了镜。新函数 `presetHits()`；打开导出中心时会重算（分镜可能在两次打开之间变了）。
- **体检卡「去修第一个」**：体检报告告诉你哪里有问题，但还得自己在几十镜里翻。新增按钮直接切到「生成图 / 视频」页、展开并平滑滚动定位到第一个**硬性缺料**的镜头，并提示它的全部问题项。

### Changed
- **剩余清单改为真正的 CSV（带 UTF-8 BOM）**：以前是无 BOM 的 TSV，Windows 版 Excel 打开中文会乱码，而且默认分隔符未必是 Tab。新增 `csvCell()` / `csvRow()`（含逗号 / 引号 / 换行的字段按 RFC 4180 加引号并把内部引号翻倍），导出文件名改为 `.csv`、MIME 为 `text/csv;charset=utf-8`，内容前补 `\ufeff`。Excel / Numbers / WPS 双击即可正确显示中文。

## [v2.20] - 2026-09-11

### Added
- **镜号表达式支持章节引用**：`#章节名` 直接把整章的镜号展开进来，如 `#第一集`、`1-100,!#第一集`（整章排除）。匹配规则是「先找完全同名的章节，找不到才退化为包含匹配」——用户常只记得片段（「第一集」），但也不能让「第一集」误命中「第十一集」。新增纯函数 `chapterShotNos()`。含 `#` 的表达式在范围标签里**直接回显原文**，不再展开成几十个镜号。
- **镜号表达式「常用预设」**：导出中心可把当前表达式存成有名字的预设（`cfg.rangePresets`），点一下即套用，× 删除；同名再存 = 覆盖。适合「只导前 20 镜」「排除片头」这类固定选区。
- **续跑卡可导出剩余清单**：新增纯函数 `buildLeftText(kind)`，把该类型的剩余镜号导成 TSV（镜号 / 章节 / 景别 / 当前状态 / **缺什么**），Excel 直接能开；镜头已被删的行会标注「（镜头已删除）」。首页续跑卡新增「导出清单」按钮。
- **关键字段体检**：新增 `shotHealth()` 逐镜体检「缺画面描述 / 无生图提示词 / 没时长 / 没章节 / 有台词没角色」，分**硬性**（拖累出图与成片时长）与**软性**（影响分集打包与音色匹配）两档。首页「现在要处理」只在**出现硬性缺料**时才弹体检卡（否则软性提示天天占首页会让人麻木），可「查看明细」（弹窗内滚动，最多 200 条）或「导出报告」（TSV）。`healthSummary()` 的排序把硬性问题排在前面。**与「跑前预检」的分工**：预检判断「这一步现在能不能跑」，体检判断「这条数据本身缺不缺料」；体检只提示、不阻断。
- **自绘输入弹窗 `askInput()`**：原生 `window.prompt` 与 `confirm` 一样会被 iframe 沙箱静默拦截，命名类操作（存预设 / 存套餐）必须有个能收字符串的对话框。回车等同确定；「确定」返回 trim 后的名称（空串视为取消），「取消」返回 null。
- **类型套餐可编辑多套**：`cfg.genrePresets` 保存用户自定义配方（名称 + 画风预设 + 全局画风令牌 + 是否注入提示词）。设置页新增「类型套餐库」：内置三套（现代 / 古装 / 动画）+ 自定义 N 套，点名称即套用（套用前弹确认说明覆盖范围），自定义的可点 × 删除，**内置套餐不给删**（避免把类型联动弄没了）。新增 `genrePresetList()/renderGenrePresets()/saveGenrePreset()/applyGenrePreset()/delGenrePreset()`。

### Changed
- **导出体积估算细化到分文件 + zip 容器开销**：`estExportSize()` 现在返回 `parts`（每个文件的估算字节）与 `biggest`（最大的一项），并把 zip 自身的开销算进去——store 模式下每个文件有本地头（30 + 名字）与中央目录（46 + 名字）两条记录，末尾还有 22 字节 EOCD。以前预览里的数字**永远比实际下载的包小一点**，现在对得上了。导出预览新增一行「体积明细（按大小）」，列出前 3 大项，方便判断「包为什么这么大」。`text` 仍保持「不勾整轨时的体积」口径，`total = text + track` 不变。

### Fixed
- 无（本版未发现回归；v2.19 的 341 项断言全数保留，仅 `parseShotRange` 的依赖图新增 `chapterShotNos` 一项，老断言零改动）。

## [v2.19] - 2026-09-11

### Changed
- **批量剩余存档从「只有一条」改为「按类型分条」**：存储结构变为 `{img:{…}, vid:{…}, audio:{…}}`，生图 / 生视频 / 配音三类各自保留自己的剩余进度，**可以同时存在、分别续跑**；以前跑配音会把生图的剩余挤掉。首页「现在要处理」区按类型各出一张续跑卡，各带「只跑剩余镜 / 去定位 / 忽略」；「忽略」只清自己那一类。读到旧的单条格式（自带 `kind` 字段）会**自动迁移**，老用户的剩余不丢。
- **`runPool` 收尾简化**：存档分条后不再需要「取现有记录比对 kind」那段逻辑，直接写 / 清自己那一条，少一处易错分支。
- **批量跑前统一预检**：生视频缺图片、配音缺台词的镜头在入口就被剔出（`preflightShots`），不再白占一个进度名额、也不被误算成失败；确认框与进度条标题都会写明「跳过 N 个（没图片 / 没台词）」，跑完后收尾文案同样带一句。
- **剧集类型联动从「换一套画风」升级为「整套配置套餐」**：新增 `GENRE_BUNDLE`（画风预设 + 全局画风令牌），一键套用会同时改画风 / 前缀 / 负向词、写入全局画风令牌、开启「类型写进提示词」，套用前弹确认说明会覆盖哪些项；类型旁的提示也改为展示整套内容。

### Added
- **自定镜号支持多段与排除**：导出中心的「自定镜号」由两个起止数字框合并为一个表达式输入框，支持 `1-20,25,30-35`（多段并集）、`1-50,!8-10`（`!` 前缀排除）、`!3`（只写排除段 = 全部扣掉）、`8-` / `-20`（开区间）；写错的段自动忽略不报错，空表达式等于不限。老配置里的「起止镜号」会自动拼成表达式迁移过来。新增纯函数 `parseShotRange` / `rangeHit` / `shotRangeTxt`。
- **导出预览增加体积估算**：范围行实时显示「预计体积 X」（含整轨时另报「含整轨 Y；不勾整轨约 Z」）。文本类按真实内容累加（字幕 / 脚本 / 清单 / 数据 / 转码命令），整轨按 wav 单声道 16bit @24k 估算——与 `buildTrackWav` 实际规格一致；镜数超过 1500 时改用均值估算，避免每敲一个字符都全量拼字符串卡住页面。新增 `fmtBytes` / `estExportSize`。

## [v2.18] - 2026-09-11

### Changed
- **批量收尾逻辑收敛到一处（`batchRun`）**：此前「生图 / 生视频」与「配音」各写一套 `batchBarOpen → runPool → 收尾` 代码，改一处容易漏一处。现抽出 `batchRun(kind, list, opts)`，三类批量与新增的「只跑剩余镜」共用同一份进度条、失败统计、候选提示与暂停/完成文案，收尾口径唯一。
- **`runPool` 的 `opts` 扩展为 `{ kind, ids }`**：`ids` 为本次批量「计划要跑的镜」ID 全集，写进剩余存档当分母。续跑卡由此能显示「共 N 个里还剩 M 个」，不再只报一个孤零零的剩余数（旧存档无 `total` 时自动回落为剩余数，向后兼容）。

### Added
- **打包范围新增「自定镜号区间」**：导出中心的打包范围增加第 5 种取值，可填起止镜号（含两端；只填一端表示从那端起 / 到那端止；两端都空等于不限），字幕 / 脚本 / 清单 / 数据 / 整轨配音同步生效，范围写进包内说明。
- **导出前即时预览**：打包范围下方实时显示「本次将导出 N / M 镜 · 预计时长 X」，时长按统一的 `shotDur` 口径累加；范围为 0 镜时给出红色警告，避免打完包才发现选错。打开导出中心、切换范围、修改区间时即时刷新。
- **剧集类型联动画风预设**：设置里「剧集画面类型」旁新增「按类型套用画风预设」按钮与「建议画风」提示——类型换成古装 / 动画时会提示该配哪套风格，一键把画风、提示词前缀、负向词同步为匹配的那套（`applyStylePreset` 同时顺带回填设置里的前缀 / 负向词输入框，修掉「只改 cfg、输入框还显示旧值，下次保存又被覆盖」的隐患）。
- **续跑卡「只跑剩余镜」直投队列**：首页「续跑」卡新增主按钮，直接把存档里的剩余镜号映射回镜头对象并投入批量队列（`resumeLeftRun`），不再依赖「重新点批量按钮 + 按未生成/失败重算列表」——中途改过设置或筛选时，重算出来的列表可能多一些或少一些，直投则与上次的剩余严格一致；镜头已被删除时自动清档并提示。

### Fixed
- **`applyStylePreset` 未回填设置输入框**：切风格预设只改了 `cfg.prefix / cfg.neg`，设置面板里的输入框仍是旧值，用户下次点「保存设置」会把 `cfg` 覆盖回旧值（等于白切）。现切换时同步输入框。

## [v2.17] - 2026-09-11

### Fixed
- **批量任务收尾崩溃（v2.16 回归）**：`runPool` 的调用点传了第 4 个参数 `{ kind: kind }`，但函数声明只写了 3 个形参，函数体末尾的 `opts && opts.kind` 实际读取了一个不存在的标识符，抛 `ReferenceError`。后果是**每次批量跑完（或暂停）时收尾逻辑全部失效**——进度面板不关闭、暂停提示不出现、剩余镜号不入档，且 `.then(function(left){...})` 的后续分支被 reject 吞掉。现补回形参 `opts`，并加入回归断言（断言函数签名含第 4 参）。
- **剧集画面类型设置刷新后丢失（v2.16 回归）**：`cfg.genre` 未登记进 `DEFAULT_CFG`，而 `loadCfg` 只回填「已存在于默认配置里的键」，导致保存过的类型在刷新后被打回「自动」。现把 `genre`（连同新增的 `genrePrompt`、`expScope`）登记进默认配置。

### Added
- **剧集画面类型扩展到生成提示词**：v2.16 的类型预设此前只影响评分阈值，现在每种类型额外带一段画风描述（中/英各一份），并注入生图（`buildImageBody`）、生视频（`buildVideoBody`）与设定图（`buildAssetBody`）的提示词——把「这是古装 / 动画 / 现代写实」提前告诉模型，避免古装人物被画成现代装、动画被画成实拍。选「自动」时描述为空、不注入，行为与 v2.16 完全一致；设置里新增开关可单独关闭。
- **一键打包的范围选择**：导出中心新增「打包范围」下拉（全部镜头 / 仅已完成 / 跟随当前筛选 / 仅勾选章节）。`buildSrtText` / `buildMdText` / `buildCsvText` / `buildTrackWav` 全部增加可选范围参数（不传即全部，向后兼容），一键打包按所选范围出字幕、脚本、清单、数据与整轨配音；包内附「打包说明.txt」写明范围与包含的镜号。选择会记入配置，下次打开仍是上次的范围。
- **批量剩余进度可视化**：首页「现在要处理」新增「续跑」卡片，显示剩余任务类型、数量与镜号预览，提供「去续跑」（切到对应筛选、展开并平滑滚动定位到第一个未完成镜头）与「忽略」（清除记录）两个操作；批量暂停或跑完时首页卡片即时同步。

### Changed
- **导出排序口径统一**：新增 `sortShots()`，字幕 / 脚本 / 清单 / 整轨一律按镜号升序，不依赖数组当前顺序。

## [v2.16] - 2026-09-11

### Added
- **一键打包导出（.zip）**：导出中心新增「一键打包导出」，把字幕 .srt、分镜脚本 .md、视频清单 .csv、全量数据 .json、FFmpeg 转码命令 .txt 一次打成一个压缩包；本机有配音时会询问是否把整轨 .wav 一并打进去。**自带极简 zip 实现**（CRC32 + 本地文件头 + 中央目录 + EOCD 的 store 模式，不做压缩），不引 JSZip 之类几百 KB 的外部依赖，保持单文件零依赖。
- **剧集画面类型预设**：设置新增「剧集画面类型」（自动 / 现代都市 / 古装年代 / 动画二次元），按题材更换主体占比与构图安全区的判定阈值——古装厚衣几乎不露肤色，会放宽「空镜」阈值、提高显著性权重；动画高饱和且光照平坦，会提高显著性门槛并放宽舒适区间。不选（自动）时行为与 v2.15 完全一致。

### Changed
- **导出内容抽成纯函数**：字幕 / md / csv 的生成逻辑抽为 `buildSrtText` / `buildMdText` / `buildCsvText`，整轨合成抽为 `buildTrackWav`——「单独导出」与「一键打包」共用同一份实现，不会出现两种导出结果不一致。
- **批量剩余进度持久化**：暂停 / 停止后没跑完的镜号写进本机存档；收尾提示会报准「还剩 N 个未完成」，重新打开页面也会提示并在对应批量按钮上挂「（续跑 N）」角标。批量入口本就按「未生成 / 失败」筛选，点一下即按未完成项续跑。存档按任务类型（生图 / 生视频 / 配音）区分，不会互相覆盖或误清。
- **暂停时不再谎报完成**：批量收尾文案区分「全部完成」与「已暂停，还剩 N 个」，避免暂停后看到「批量完成」的误判。

### Fixed
- 顺带修正了整轨导出里 `sr` 的冗余赋值（采样率取首个有效音频片段，逻辑不变但更清晰）。

## [v2.15] - 2026-09-10

### Added
- **导出中心**：原先挤在「④ 成片与导出」工具区的一排导出按钮（md / csv / json / srt / 整轨配音 / mp3 转码命令 / 一键合成包 / fcpxml）集中到一个面板，按 **① 出片 ② 字幕与音频 ③ 数据与脚本 ④ 备份与恢复** 四组排列；工具区只留「导出中心」入口与「批量生成全部配音」。面板里的按钮沿用原 id 与事件绑定，行为零变化；备份 / 导入 / 清空转发到首页那套逻辑，不重复实现。
- **批量任务暂停 / 继续**：批量进度条新增「暂停」按钮（暂停后不再取新任务，正在跑的几项跑完即停住，点「继续」接着跑）。停工 / 停页面都不用怕——批量入口本来就按「未生成 / 失败」筛选，重新点一次批量就是**按未完成项续跑**，停止提示里也写明了这一点。

### Changed
- **候选图「人物主体占比」改用肤色 ∪ 显著性**：v2.14 只看肤色（YCbCr 区间），暗调夜戏 / 古装厚衣 / 强色偏会被误报成「看不到人物」。现在改为取「肤色占比」与「显著性占比（亮度偏离全图均值 + 边缘强度，打 0.6 折）」的较大者——显著性对人物不依赖肤色，能兜住暗调场景；纯风景空镜两者都极低，仍会给出「几乎看不到人物」的提示。依然是像素统计、不是人脸检测，只作提示。

### Fixed
- **导出中心 id 冲突**：新面板最初用了 `exBox` / `exMask` / `exClose`，与既有的「AI 提取结果弹窗」完全同名——`getElementById` 只会返回第一个，原弹窗会被静默劫持。已全部改为 `expBox` / `expMask` / `expClose`，并新增**结构性防回归断言**：HTML 内 id 全局唯一 + 所有 `$('id')` 引用都必须真实存在。

## [v2.14] - 2026-09-10

### Added
- **候选图评分新增「人物主体占比」维度**：用经典 YCbCr 肤色区间统计肤色像素比例，免模型地近似「人在画面里占多大」，挑掉两头的废片——比例过低提示「几乎看不到人物（可能是空镜）」「人物主体偏小」，过高提示「主体怼满画面（特写过近）」，面板显示「主体 N」分。设置里可单独关。**边界说清楚**：这是像素颜色统计、不是人脸检测，古装 / 暗调 / 强色偏 / 非人物空镜都会偏低，只作提示不替用户决定。
- **批量任务进度条**：批量配音、批量生图 / 生视频不再用 toast 刷「进度 3/20」，改为常驻进度面板——标题 + 已完成/总数 + **失败数** + 进度条 + 「停止」按钮，完成后给出「成功 X 条，失败 Y 条」的收尾提示（失败可点「重试失败」）。
- **mp3 转码命令一键复制**：导出工具区新增「复制 mp3 转码命令」，给出可直接在导出目录执行的 FFmpeg 命令（含整轨单文件与分镜多文件批量两种写法），导出 wav 后也会提示。**不引入 lamejs**：几百 KB 内联不现实、走 CDN 破坏离线可用，一键合成包本来就用 FFmpeg，等于零额外依赖。

### Changed
- **候选图综合分改为四维加权并规则化**：一致性 20% / 构图 20% / 主体 15% 各自固定，剩余权重全归技术分（全开时 = 技术 45% + 一致性 20% + 构图 20% + 主体 15%）。关掉某个维度，它那部分自动还给技术分——不会因为缺维度凭空拉低总分。副作用：只开一致性时由原来的 75/25 变为 80/20。
- **批量并发池回传当前项**：`runPool` 的进度回调多带一个 item 参数，让调用方能在每项完成时统计失败数（此前只能报已完成数）。

## [v2.13] - 2026-09-10

### Added
- **候选图评分新增「构图安全区」维度**：检查三个最容易在实际出片时翻车的构图问题——① 底部字幕区是否过挤（下 18% 区域的边缘密度明显高于全图，字幕压上去会糊）；② 主体是否被画框切断（最外一圈的梯度远高于全图，通常是半张脸 / 半只手贴边）；③ 内容重心是否偏出画面（用「偏离全图均值的程度 + 边缘强度」加权算重心）。命中会给出「底部过挤（字幕会压住主体）」「主体贴边被裁」「主体偏离画面中心」标签并在面板显示「构图 N」分。设置里可单独关。
- **逐句配音单句失败自动重试**：并发合成时某一句遇到限流 / 抖动不再直接判死整镜，按全局「失败自动重试」次数做退避重试（1.2s / 2.4s / 3.6s），全部重试仍失败才整镜失败，错误信息明确指向第几句。

### Changed
- **候选图综合分改为三维加权**：技术分 / 一致性 / 构图同时在时为 60% / 20% / 20%；缺哪个维度就把那部分权重还给技术分，不会因缺失维度而凭空拉低总分（关掉全部附加维度时行为与 v2.11 的纯技术分完全一致）。
- **整轨压缩导出优先通用容器**：原来只会输出 .webm/opus，现在优先尝试 **audio/mp4（AAC）**——剪映、PR、达芬奇都直接认，不支持的浏览器再退回 opus，最后退回 wav，文件名按实际编码给 `.m4a` / `.webm`。仍然不引入任何外部编码库，保持单文件零依赖。

## [v2.12] - 2026-09-10

### Added
- **候选图评分新增「画风一致性」维度**：用色彩直方图（RGB 各量化 4 级 = 64 桶）比对候选图与本剧最近定稿镜头的平均色调，得出一致性分并计入综合分（技术分 75% + 一致性 25%），面板上显示「· 一致性 N」并加「调性一致 / 与全剧色调有偏差」标签。需要说明的是：真正的语义一致性需要 CLIP 之类的模型、纯前端做不到，色调分布是目前最接近的可算代理，只用来提示不用来替你决定。设置里可单独关掉（关掉后综合分等于纯技术分，行为与 v2.11 完全一致）。
- **逐句配音并发提速**：设置新增「逐句配音并发路数」（1 / 2 / 3，默认 2）。原来逐句是串行请求，多句台词一镜要等好几轮；现在按配置并发合成，结果**按句序下标回填**后再拼接，顺序绝不会乱，缺句的镜头也不会串位。
- **整轨配音可选导出压缩版**：设置新增「整轨配音导出压缩版」（默认关），开启后导出 .webm/opus 而不是 wav，体积约为 wav 的 1/10，方便传网盘或拖进剪映。默认关的原因写在设置说明里：压缩必须实时录制，耗时≈成片总长；浏览器不支持时自动改存 wav 并提示。

### Changed
- `imgStatsFromData` 顺带返回色彩直方图，避免同一张图重复解码取像素。

## [v2.11] - 2026-09-10

### Added
- **候选图自动技术评分**：A/B 选优面板里给每张候选图算 0–100 分并给一句点评（清晰度 40 / 曝光 25 / 对比度 20 / 色彩饱和度 15），最高分标「★ 推荐」且选中按钮变为「✓ 选 A（推荐）」。只评客观技术项（偏糊 / 偏暗 / 过曝 / 发灰 / 色彩寡淡），审美判断留给人；设置里可关。跨域图取不到像素时显示「—（取不到像素，未评分）」，不影响选图流程。
- **逐句配音压成压缩格式存本机**：设置新增「逐句配音拼好后压成压缩格式再存本机」（默认开）。用 MediaRecorder 把拼接好的 PCM 编成 webm/opus 再落 localStorage，体积约为 wav 的 1/10，本机可存配音量提升一个数量级；浏览器不支持或编码失败自动退回 wav，并有 8 秒兜底超时防止流程吊死。
- **cue 级字幕与口型时间对齐**：设置新增「用配音真实句长对齐」（默认开）。跑过逐句配音的镜头，导出字幕时按每句**实际念了多少秒**切分时间轴（句间 0.12 秒静音归给前一句，保证各句之和等于整段时长），字幕与嘴型严丝合缝；没配音或台词改过导致句数对不上时自动退回按字数估算。导出完成会提示「其中 N 镜按配音真实句长对齐」。

### Changed
- 每句真实时长存本机独立账本（`LS + 'cuedur_' + id`），与音频一样不落资料库；删除分镜时一并清理，不会残留脏数据。

## [v2.10] - 2026-09-10

### Added
- **批量 / 流水线接入 A/B 选优**：设置新增「批量生图 / 长篇流水线也按候选数生成」开关。开启后批量生图与流水线对每个镜头静默生成 N 版候选（不弹面板打断跑批），跑完提示「N 个镜头的候选图待挑选」，逐镜点「选图(N)」挑；成本预估会按候选数乘算，不会悄悄多扣钱。
- **cue 级 TTS（逐句配音）**：设置新增「逐句配音」开关（默认关）。开启后多句台词按 cue 逐句分别合成——每句用自己说话人的角色音色与情绪（OS 加画外音演法、VO 加内心独白演法），句间插 0.12 秒静音拼接，整段降采样到 24k 后以 wav 落本机。解决 v2.9 质检新发现的「一镜多说话人单音色读串」，无需被迫拆镜。代价：每句一次请求 + wav 体积远大于 mp3，故默认关闭并在提示里说明。
- **拆镜 / 剧本化模型直出 cue 格式台词**：拆镜系统提示词新增规则 10，要求 `line` 直接输出 `【情绪】角色名：台词`、`角色名(OS)/(VO)：台词`、`旁白：台词`，一句一行、单句不超过 20 字（长了自动断句）；剧本化提示词同步要求对白写成「角色名：台词」。结构化从源头成立，不用事后逐句编辑。

### Changed
- 质检「一镜多说话人」告警的建议改为：拆成多镜，或开启「逐句配音」按各自音色合成。

### 技术说明
- 新增 `concatAudioBufs` / `resampleBuf` / `abToB64` / `cueVoiceStyle` / `genAudioCue` / `genShotImage`；`genImageCand(id, silent, n)` 支持静默批量模式。
- 回归单测扩到 **41 项断言**（新增音频拼接布局与静音间隙、abToB64 分块、批量生图路由 4 种组合、提示词含 cue 规则），全部通过。

## [v2.9] - 2026-09-10

### Added
- **多版本一次生成 A/B 选优**：分镜卡新增「A/B 选优」按钮，一次并发生成 2~3 版候选图（设置里配候选数），弹出面板并排对比、点「选它」落为当前图；落选候选连同旧图自动进入 v2.7 的图像版本历史（⇄ 可回退），不静默丢弃任何一张已生成的图。关掉面板后镜卡会保留「选图(N)」入口，随时回来继续挑。
- **台词逐句 cue 结构化**：「台词旁白」仍是唯一落库字段（老项目零迁移），新增结构化序列化约定 `【情绪】角色名(OS)：台词`（OS=画外音 / VO=内心独白 / 旁白=旁白）。新增「逐句编辑」弹窗：逐句设置说话人（带角色卡名称联想）、类型（对白/旁白/OS/VO）、情绪与断句，保存回写台词字段。三处消费：
  - **导出字幕**：一键按逐句 cue 拆条（可在设置关闭），时间按各句预计语速占比在本镜时长内分配、尾句兜到镜末不留缝，对白带「角色名：」前缀；纯长文本自动按句号断句。
  - **配音（小米 TTS）**：「说话角色」留空时自动取第一个对白 cue 的角色音色；每句【情绪】逐句注入 TTS 风格指令；送 TTS 的文本剥掉「角色名：」前缀（音色已区分说话人，不念名字），多句用换行连接形成句间停顿。
  - **分镜质检**：新增「一镜多说话人」告警（单音色会读串，建议拆镜）与「超长句（>20 字）」断句提示。

### Changed
- 设置新增「生图候选数」（1/2/3，1=不启用 A/B）与「逐句字幕」开关（默认开）。

### 技术说明
- 候选图 `_imgCand`、版本历史 `_imgHist` 均为 `_` 前缀本地字段，不落资料库、不破坏 schema。
- `parseCues / fmtCues / cueEst / cuePrefix` 纯函数与 SRT 分配逻辑经 27 项断言单测全通过；`parseCues` 对无前缀老文本完全向后兼容（整段视为旁白，长文本自动断句）。

## [v2.8] - 2026-09-10

### Added
- 项目级「全局统一画风令牌」（`cfg.globalStyle`，设置抽屉新增输入框）：对标 Jellyfish 的全局风格防漂移。填一段画风/年代/材质/光质描述后，会固定注入每一个镜头的生图与生视频提示词，让整部剧从第一镜到最后一镜保持同一视觉调性。

### Fixed
- 统一镜头有效时长口径（`shotDur` 优先音频真实时长并 clamp 到 API 允许的 4–12 秒），修复了 render.sh 段长 / SRT / 整轨配音 / FCPXML 四处时长各自计算、在有配音的镜上可能对不齐导致音画/字幕漂移的遗留缺陷。
- 生成配音时把「音频时长」也 clamp 到 4–12 并回写，与 `时长秒`、render.sh 段长保持同一数值，杜绝超长语音在剪辑软件里拖长字幕的隐患。

## [v2.7] - 2026-09-09

细粒度质量与生产增强（对标火宝 v3.1 受控镜头语言、MoneyPrinterTurbo 字幕底与多版本选优）。

- 修复：合成包 render.sh 与 FCPXML 时间线的 seg 段号口径不一致 —— 有缺视频镜时两边 `seg_N.mp4` 会错位、剪辑导错镜头；现统一按「有图片或有视频」的镜连续编号
- 修复：重生成某镜图片会静默覆盖旧图、且不清下游 —— 引入同镜图像版本历史，旧图先入历史不丢失，可在镜卡一键「切回上一版」；已换图且有旧视频时给出「重新生视频」警示，不再静默出废片
- 修复：配音 base64 不随备份走，换机/导入后配音必丢 —— 导出 JSON 可选用「含配音」，导入按镜号自动写回
- 新增：镜头语言受控词表（机位角度 / 运镜 中→英双语映射），生图与生视频提示词自动输出规范英文 camera 语言（未命中词保留原文，不打断自由创作）
- 新增：字幕可选「半透明黑底」（对标 MoneyPrinterTurbo 字幕底，ASS BorderStyle=4 + BackColour），设置里开关、烧录时生效
- 技术：`toProps` 忽略 `_` 前缀本地字段（候选图历史等不落资料库、不破坏 schema）

## [v2.6] - 2026-09-09

第三次细粒度对标 GitHub 高星项目（火宝短剧角色提取 Agent、魔因漫创质检 Agent 与字幕样式、ArcReel）后的改进。

- 新增 AI 从小说一键提取角色与场景：读全文输出结构化设定卡（发型/上下装/配饰/性格/英文锚点/TTS音色），预览勾选后批量写入，已存在自动跳过
- 新增分镜质检 Agent：逐镜体检时长越界、台词念不完/拖沓、缺角色场景卡与设定图、生成失败、口型风险、连续同景别、全片过短，每条带一键修复
- 分镜新增光影/时间/天气字段并注入画面提示词；拆镜模型同步输出 lighting
- 角色外观改为结构化（发型/上装/下装/配饰道具），无常服时自动拼装服装锁，定妆照与分镜口径一致
- 新增提示词查看/改写：看最终送给模型的完整提示词，可改写覆盖、可一键还原
- 字幕样式可配：字体/字号/颜色/描边/边距/加粗，应用到合成脚本烧录
- 新增导出剪辑时间线 .fcpxml：可直接导入 Premiere/达芬奇/Final Cut，镜头顺序与时长已排好
- 回归验证 43 项断言全部通过

## [v2.5] - 2026-09-09

第二次对标 GitHub 高星项目（Toonflow、魔因漫创 moyin-creator）后的改进。

- 新增并发任务队列：批量生图/视频/配音/口型改为并发执行，可调并发路数与失败自动重试，支持停止当前批量
- 新增多 API Key 轮换：多个 Key 每行一个，轮询使用，遇 429/401/403 自动换 Key 重试
- 新增风格预设库：8 套视觉风格，一键设定画风、提示词前缀与负向词
- 分镜编辑增强：上移/下移/复制/插入空镜并自动重编号，支持撤销（按钮 + Ctrl+Z）
- 合成脚本增强：图片垫段 Ken Burns 运镜、段内淡入淡出转场、帧率与码率可配（转场不改变总时长，音画不错位）
- 回归验证 22 项断言全部通过，生成脚本通过 bash 语法校验

## [v2.4] - 2026-09-09

v2.3 代码完整性审计修复。

- 修复：章节「AI 剧本化」结果不持久化，刷新页面后全部丢失、流水线退回原文拆镜的问题
- 新增防串拆保护：重新识别章节时，文本变化的章节自动弃用旧剧本
- 提升：流水线启动前的视频成本预估改按真实镜头时长累计
- 回归验证 11 项断言全部通过

## [v2.3] - 2026-09-09

对标 GitHub 高星短剧项目（huobao-drama、NarratoAI、ArcReel 等）补齐功能差距。

- 新增一键合成包导出：render.sh / render.bat / 合成清单.json，ffmpeg 完成视频拼接、图片垫段、人声+BGM 混音、字幕烧录，产出成片.mp4
- 新增 BGM 背景音乐轨：URL / 音量 / 淡入淡出配置，WebAudio 混入整轨配音
- 新增用量与成本追踪：图 / 视频秒 / 配音 / 口型自动计数，可填单价，实时预估花费
- 新增章节 AI 剧本化：正文改写为对白化、强钩子剧本后再拆镜，支持还原
- 批量生成（图 / 视频 / 配音 / 口型 / 流水线）前新增成本确认弹窗

## [v2.2] - 2026-09-09

全面代码审计，修复 11 处问题。

- 修复参考图 URL 重复导致的「picture N」说明错位（按 URL 去重）
- 修复 URL 形态配音无真实时长的问题，音画同步与字幕时间轴随之修正
- 修复刷新后「生成中」状态卡死（自动重置，视频凭任务 ID 续轮询）
- 修复 16:9 媒体框被裁切、清空后示例复活、音频缓存泄漏、视频时长越界、设定图双击重复计费、唇形 Key 清理遗漏等

## [v2.1] - 2026-09-09

长篇流水线与口型同步。

- 新增长篇流水线：整本小说自动切章，一键跑完拆镜 → 设定卡 → 设定图 → 分镜图 → 配音 → 视频，跨章词表锁名，断点续跑
- 新增口型同步方案：风险判定 → AI 规避改写（过肩/侧脸/背身）→ 可选第三方唇形服务

## [v2.0] - 2026-09-08

v2 主版本基线（后续 v2.1–v2.6 均在此之上小版本递进）。

一致性方案 2.0 与配音。

- 一致性加固：多视图设定图（角色面部特写 / 场景反打机位，以主图为参考生成）、参考图优先级与上限可配、满意基准帧机制、服装/景别/情绪/负向词多重文字锁定、一致性体检面板
- 新增小米 MiMo TTS 配音：角色绑定音色，镜头时长自动对齐语音长度，字幕 .srt 与整轨配音 .wav 共用同一时间轴

## [v1] - 2026-09-08

首个版本。

- 剧本拆镜 → 设定图 → 分镜图 → 图生视频 → 导出的基础流水线
- 单文件 HTML + WorkBuddy 资料库云端同步，本地零依赖 CORS 代理
- 修复：iframe 沙箱内原生 confirm 失效导致的删除按钮无响应（改为自定义确认弹窗）
